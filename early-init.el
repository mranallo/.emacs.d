;;; package --- Summary
;;; Commentary:
;;; I am not sure how this works, but it is the first init file read.
;;; Code:
;; (add-to-list 'default-frame-alist '(undecorated-round . t))
(provide 'early-init)
;;; early-init.el ends here
